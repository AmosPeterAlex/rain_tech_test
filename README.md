# Raintech Hotel Management — Flutter Web

**Candidate:** Amos P Alex  
**Stack:** Flutter Web · flutter_bloc · Equatable · Intl

---

## How to Run

```bash
flutter pub get
flutter run -d chrome
```

The app opens at `http://localhost:PORT` in Chrome.

> **Tip:** Use `flutter run -d chrome --web-port 8080` to fix the port.
